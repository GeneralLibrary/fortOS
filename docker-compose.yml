# GORT self-deployment reference (see docs/gort-architecture.md §13.2)
services:
  gort-core:
    image: gort/core:latest
    build: .
    container_name: gort-core
    restart: unless-stopped
    network_mode: host
    pid: host
    privileged: true
    volumes:
      - /srv/nas:/srv/nas
      - /var/run/docker.sock:/var/run/docker.sock
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /dev:/host/dev
    environment:
      - GORT_CONFIG_PATH=/srv/nas/config/nas.yaml
      - GORT_DATA_ROOT=/srv/nas
      - ASPNETCORE_URLS=http://0.0.0.0:5000
      - ASPNETCORE_ENVIRONMENT=Production

  gort-loki:
    image: grafana/loki:latest
    container_name: gort-loki
    restart: unless-stopped
    volumes:
      - /srv/nas/logs/loki:/loki
    command: -config.file=/etc/loki/local-config.yaml
