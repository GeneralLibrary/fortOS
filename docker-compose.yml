# FortOS self-deployment reference (see docs/fortos-architecture.md §13.2)
services:
  fortos-core:
    image: fortos/core:latest
    build: .
    container_name: fortos-core
    restart: unless-stopped
    network_mode: host
    pid: host
    privileged: true
    volumes:
      - /srv/nas:/srv/nas
      - /var/run/docker.sock:/var/run/docker.sock
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /dev:/host/dev
    environment:
      - FortOS_CONFIG_PATH=/srv/nas/config/nas.yaml
      - FortOS_DATA_ROOT=/srv/nas
      - ASPNETCORE_URLS=http://0.0.0.0:5000
      - ASPNETCORE_ENVIRONMENT=Production

  fortos-loki:
    image: grafana/loki:latest
    container_name: fortos-loki
    restart: unless-stopped
    volumes:
      - /srv/nas/logs/loki:/loki
    command: -config.file=/etc/loki/local-config.yaml
